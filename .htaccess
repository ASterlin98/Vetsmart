RewriteEngine On

# Si el archivo o directorio solicitado no existe físicamente, envía la petición a index.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [L]